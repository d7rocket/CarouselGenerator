---
phase: 03-export-copy
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [index.html]
autonomous: false
requirements: [COPY-01, COPY-02]

must_haves:
  truths:
    - "User can copy the caption text with one click on a clipboard icon button"
    - "User can copy the hashtags text with one click on a clipboard icon button"
    - "After copying, the icon changes to a checkmark for ~2 seconds then reverts"
    - "Copy buttons are positioned at the top-right corner of their respective text blocks"
  artifacts:
    - path: "index.html"
      provides: "Copy-to-clipboard buttons for caption and hashtags blocks"
      contains: "navigator.clipboard.writeText"
  key_links:
    - from: "caption copy button click"
      to: "navigator.clipboard.writeText"
      via: "reads caption text from parsed.caption"
      pattern: "clipboard.writeText"
    - from: "hashtags copy button click"
      to: "navigator.clipboard.writeText"
      via: "reads hashtags text from parsed.hashtags joined"
      pattern: "clipboard.writeText"
---

<objective>
Add clipboard copy buttons for caption and hashtags text blocks, plus a final human verification checkpoint.

Purpose: Users need one-click copying of their caption and hashtags to paste directly into Instagram. This completes the full workflow: upload -> theme -> export -> copy.
Output: Two inline copy icon buttons with visual feedback, and verified end-to-end functionality.
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-export-copy/03-CONTEXT.md
@.planning/phases/03-export-copy/03-01-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add copy-to-clipboard buttons for caption and hashtags</name>
  <files>index.html</files>
  <read_first>index.html (full file -- understand the caption block construction at ~line 1314-1327 and hashtags block at ~line 1329-1344 inside renderResults; also see existing CSS patterns for button styling)</read_first>
  <action>
  1. Add CSS styles inside the existing `<style>` block for the copy button and its container. The caption-block and hashtags-container need `position: relative` added to their existing rules so the copy button can be absolutely positioned:
     ```
     .caption-block, .hashtags-container { position: relative; }
     .copy-btn {
       position: absolute;
       top: 0.5rem;
       right: 0.5rem;
       background: rgba(255,255,255,0.8);
       border: 1px solid #E0DCD4;
       border-radius: 6px;
       padding: 0.35rem;
       cursor: pointer;
       display: flex;
       align-items: center;
       justify-content: center;
       transition: background 0.2s, border-color 0.2s;
       width: 28px;
       height: 28px;
     }
     .copy-btn:hover { background: #F5F0E8; border-color: #C8A96E; }
     .copy-btn svg { width: 14px; height: 14px; }
     ```

  2. Create a helper function `createCopyButton(textToCopy)` placed in the script section (as a top-level function, before renderResults). This function:
     - Creates a `<button>` element with class `copy-btn` and `aria-label="Copy to clipboard"`.
     - Creates a clipboard SVG icon using `document.createElementNS('http://www.w3.org/2000/svg', 'svg')`. The clipboard icon path data:
       - viewBox="0 0 16 16"
       - `<path d="M5 2H4a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2h-1" stroke="#6B6560" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>`
       - `<rect x="5" y="1" width="6" height="3" rx="1" stroke="#6B6560" stroke-width="1.5" fill="none"/>`
     - Creates a checkmark SVG icon (initially hidden, display:none):
       - viewBox="0 0 16 16"
       - `<path d="M3 8.5L6.5 12L13 4" stroke="#4A9E4A" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>`
     - Appends both SVGs to the button (clipboard visible, checkmark hidden).
     - Adds click event listener that:
       a. Calls `navigator.clipboard.writeText(textToCopy)`.
       b. On success (`.then`): hides the clipboard SVG (`style.display = 'none'`), shows the checkmark SVG (`style.display = ''`).
       c. After 2000ms (`setTimeout`), reverts: shows clipboard SVG, hides checkmark SVG.
       d. On failure (`.catch`): does nothing (silent fail -- clipboard API may not be available in all contexts).
     - Returns the button element.

  3. In the `renderResults` function, modify the CAPTION section (currently ~line 1314-1327). After creating the `capBlock` div and appending caption text to it, but BEFORE appending `capBlock` to `results`:
     - Create the copy button: `var capCopyBtn = createCopyButton(parsed.caption);`
     - Append it to the capBlock: `capBlock.appendChild(capCopyBtn);`
     - Add right padding to the capBlock so text doesn't overlap the button: `capBlock.style.paddingRight = '2.5rem';`

  4. In the `renderResults` function, modify the HASHTAGS section (currently ~line 1329-1344). After creating the `tagContainer` div and appending all hashtag pills to it, but BEFORE appending `tagContainer` to `results`:
     - Build the hashtags text string for copying: `var hashtagsText = parsed.hashtags.join(' ');`
     - Create the copy button: `var tagCopyBtn = createCopyButton(hashtagsText);`
     - Append it to the tagContainer: `tagContainer.appendChild(tagCopyBtn);`
     - Add right padding: `tagContainer.style.paddingRight = '2.5rem';`

  5. Add inline smoke tests in the existing smoke test section:
     - After renderResults runs, `document.querySelectorAll('.copy-btn').length` equals 2 (one for caption, one for hashtags)
     - `typeof createCopyButton === 'function'`
     - Each `.copy-btn` contains exactly 2 `<svg>` elements
     - The first SVG in each copy-btn has `display` not equal to 'none' (clipboard icon visible)
     - The second SVG in each copy-btn has `display` equal to 'none' (checkmark hidden)
  </action>
  <verify>
    <automated>grep -c 'createCopyButton' index.html && grep -c 'navigator.clipboard.writeText' index.html && grep -c 'copy-btn' index.html && grep -c 'capCopyBtn' index.html && grep -c 'tagCopyBtn' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `function createCopyButton` that accepts a text string parameter
    - index.html contains `navigator.clipboard.writeText(textToCopy)` inside the click handler
    - index.html contains CSS class `.copy-btn` with `position: absolute`, `top: 0.5rem`, `right: 0.5rem`
    - index.html contains `.caption-block, .hashtags-container` rule with `position: relative`
    - index.html contains `var capCopyBtn = createCopyButton(parsed.caption)` and `capBlock.appendChild(capCopyBtn)`
    - index.html contains `var hashtagsText = parsed.hashtags.join(' ')` and `createCopyButton(hashtagsText)`
    - index.html contains `setTimeout` with 2000 ms delay for reverting checkmark to clipboard icon
    - Checkmark SVG path contains `stroke="#4A9E4A"` (green color)
    - Clipboard SVG path contains `stroke="#6B6560"`
    - Smoke tests check `.copy-btn` count equals 2 and SVG visibility states
    - All DOM construction uses createElement/createElementNS (no innerHTML)
  </acceptance_criteria>
  <done>Caption block has a clipboard icon button at top-right that copies caption text. Hashtags block has a clipboard icon button at top-right that copies all hashtags joined by spaces. Clicking either swaps icon to green checkmark for 2 seconds then reverts. Smoke tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full export and copy workflow</name>
  <files>index.html</files>
  <action>Human verification checkpoint. No code changes -- user tests the complete Phase 3 functionality end-to-end in a browser.</action>
  <what-built>Complete Phase 3 export and copy functionality: single-slide PNG download, ZIP of all slides, caption copy, hashtags copy, loading states, file naming</what-built>
  <how-to-verify>
    1. Open index.html in Chrome/Firefox
    2. Upload a test markdown file (or use the existing test content if available)
    3. Select a theme and navigate to a middle slide (e.g. slide 3)
    4. EXPORT - Single slide:
       - Click "Download this slide" button
       - Verify button shows spinner + "Exporting..." while processing
       - Verify a PNG downloads with name like `{topic}-slide-03.png`
       - Open the PNG -- confirm it is exactly 1080x1080 pixels (check in image properties)
       - Confirm text is crisp, theme colors are correct, no visual artifacts
    5. EXPORT - ZIP:
       - Click "Download all slides as ZIP" button
       - Verify button shows spinner + "Exporting..." while processing
       - Verify a ZIP downloads with name like `{topic}-carousel.zip`
       - Extract the ZIP -- confirm all slides are present (slide-01 through slide-0N)
       - Open a few PNGs -- confirm each is 1080x1080 with correct theme rendering
    6. COPY - Caption:
       - Look at the caption text block -- confirm a small clipboard icon appears at top-right
       - Click the clipboard icon
       - Paste somewhere (e.g. text editor) -- confirm the caption text was copied
       - Confirm the icon changed to a green checkmark, then reverted after ~2 seconds
    7. COPY - Hashtags:
       - Look at the hashtags block -- confirm a small clipboard icon appears at top-right
       - Click the clipboard icon
       - Paste somewhere -- confirm all hashtags were copied (space-separated)
       - Confirm checkmark feedback works the same way
    8. Open browser console -- confirm no errors, smoke tests pass
  </how-to-verify>
  <verify>User confirms all export and copy functionality works correctly in browser</verify>
  <done>User has approved the full Phase 3 workflow: export PNGs, download ZIP, copy caption, copy hashtags</done>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Caption copy button exists at top-right of caption block
2. Hashtags copy button exists at top-right of hashtags container
3. Clicking either copies the correct text to clipboard
4. Icon swaps to checkmark for ~2 seconds then reverts
5. Full end-to-end: upload -> theme -> navigate -> export single -> export ZIP -> copy caption -> copy hashtags all work
6. Smoke tests pass in console
</verification>

<success_criteria>
- Both copy buttons render correctly with clipboard icons
- Clipboard API copies correct text for caption and hashtags
- Visual feedback (checkmark swap) works with ~2 second duration
- All Phase 3 requirements (EXPORT-01..04, COPY-01, COPY-02) satisfied
- No console errors, all smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-export-copy/03-02-SUMMARY.md`
</output>
