---
phase: 02-theme-selection-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, PREV-01, PREV-03]

must_haves:
  truths:
    - "User sees 8 theme swatches in a horizontal row above the carousel"
    - "Clicking a swatch instantly changes the slide preview colors and fonts"
    - "Slides render at 1080x1080 and display scaled to ~420px in the viewport"
    - "Cover slide (slide 1) shows large italic title, body slides show title+text, CTA slide (last) shows call-to-action"
    - "Editorial theme is pre-selected when a file loads"
  artifacts:
    - path: "index.html"
      provides: "Theme CSS custom properties, swatch picker, carousel viewport, slide frame rendering"
      contains: "data-theme"
  key_links:
    - from: "renderResults()"
      to: "buildThemePicker() and buildSlideDOM()"
      via: "function calls in renderResults"
      pattern: "buildThemePicker|buildSlideDOM"
    - from: "theme swatch click"
      to: "slide-canvas data-theme attribute"
      via: "setAttribute('data-theme', themeId)"
      pattern: "setAttribute.*data-theme"
    - from: "CSS [data-theme] selectors"
      to: "slide content elements"
      via: "CSS custom properties (--slide-bg, --slide-accent, etc.)"
      pattern: "--slide-bg|--slide-accent|--slide-heading-color"
---

<objective>
Add 8 theme CSS definitions, a swatch picker, and a carousel preview that replaces the Phase 1 slide cards. After this plan, uploading a markdown file shows a themed 1080x1080 carousel preview with the first slide visible and all 8 themes switchable via swatches.

Purpose: This is the visual foundation for Phase 2 -- themes and slide rendering must exist before navigation controls can be added.
Output: Updated index.html with theme system, swatch picker, and carousel viewport showing the active slide.
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-theme-selection-preview/02-CONTEXT.md
@.planning/phases/02-theme-selection-preview/02-RESEARCH.md
@.planning/phases/02-theme-selection-preview/02-UI-SPEC.md
@.planning/phases/01-markdown-input-and-parsing/01-01-SUMMARY.md
@index.html
@solar_fuels_carousel_v2.html

<interfaces>
<!-- From index.html (Phase 1): -->
parseMarkdown(text) returns:
  { title, date, field, sourcesCount, slides[], caption, hashtags[], sources[], images[] }
  where each slide = { number, title, body }

renderResults(parsed, filename) -- currently renders slide cards, will be rewritten

Existing CSS classes to preserve/reuse:
  .results-header, .filename-pill, .upload-new-link, .caption-block, .hashtags-container, .hashtag-pill

DOM: #results container, #uploadZone, #fileInput
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme CSS definitions and Google Fonts import for all 8 themes</name>
  <files>index.html</files>
  <read_first>
    - index.html (current CSS structure, existing @import)
    - .planning/phases/02-theme-selection-preview/02-UI-SPEC.md (theme palette table, accent element styles, spacing scale, typography)
    - .planning/phases/02-theme-selection-preview/02-RESEARCH.md (theme palette definitions table, Google Fonts import URL, CSS custom property pattern)
  </read_first>
  <action>
In the existing `<style>` block of index.html:

1. **Replace the Google Fonts @import** (line 8) with the full import covering all 8 theme font families:
```
@import url('https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,600;1,400&family=Inter:wght@300;400;500&family=Playfair+Display:wght@400;600&family=Source+Sans+3:wght@300;400;500&family=Space+Grotesk:wght@400;500;600&family=Space+Mono:wght@400;700&family=DM+Sans:wght@300;400;500&family=Bitter:wght@400;600&family=JetBrains+Mono:wght@400;500&display=swap');
```

2. **Add CSS custom property definitions** for all 8 themes using `[data-theme]` selectors on `.slide-canvas`. Each theme defines these 8 custom properties: `--slide-bg`, `--slide-accent`, `--slide-heading-color`, `--slide-body-color`, `--slide-muted`, `--slide-border`, `--slide-font-heading`, `--slide-font-body`.

Theme values (exact hex/font values -- copy verbatim):
- `.slide-canvas` (default/editorial): bg #FAF8F4, accent #C8A96E, heading #1A1714, body #4A4540, muted #9E8C6A, border #D0CCC4, heading-font 'Lora', serif, body-font 'Inter', sans-serif
- `[data-theme="nature"]`: bg #F2F5EE, accent #6A8A5A, heading #1A2E14, body #3A4A34, muted #7A9A6A, border #C8D4C0, heading-font 'Bitter', serif, body-font 'Source Sans 3', sans-serif
- `[data-theme="deep-space"]`: bg #0E0F14, accent #4A9EFF, heading #E8ECF2, body #A0A8B8, muted #5A6478, border #1E2230, heading-font 'Space Grotesk', sans-serif, body-font 'Inter', sans-serif
- `[data-theme="minimal"]`: bg #FFFFFF, accent #1A1A1A, heading #1A1A1A, body #4A4A4A, muted #8A8A8A, border #E0E0E0, heading-font 'DM Sans', sans-serif, body-font 'DM Sans', sans-serif
- `[data-theme="sunset"]`: bg #FBF3E8, accent #C4602A, heading #2A1A0A, body #5A4030, muted #A07850, border #D8C8B0, heading-font 'Playfair Display', serif, body-font 'Source Sans 3', sans-serif
- `[data-theme="ocean"]`: bg #F0F4F7, accent #2A8A8A, heading #0A2A2A, body #2A4A4A, muted #5A8A8A, border #C0D4D4, heading-font 'DM Sans', sans-serif, body-font 'Inter', sans-serif
- `[data-theme="botanical-dark"]`: bg #0D1F0F, accent #D4C9A8, heading #F2EFE4, body #C8C0A8, muted #8A8A6A, border #1A3A1A, heading-font 'Playfair Display', serif, body-font 'Source Sans 3', sans-serif
- `[data-theme="tech"]`: bg #111418, accent #00D4AA, heading #E0E8F0, body #A0B0C0, muted #5A6A7A, border #1A2028, heading-font 'JetBrains Mono', monospace, body-font 'Space Grotesk', sans-serif

3. **Add theme swatch picker CSS** (`.theme-picker-row`, `.theme-swatch`, `.swatch-half`, `.swatch-label`):
- `.theme-picker-row`: display flex, gap 16px, overflow-x auto, `-webkit-overflow-scrolling: touch`, `scrollbar-width: none`, margin-bottom 16px. Add `::-webkit-scrollbar { display: none }` rule.
- `.theme-swatch`: button element, 48px wide x 32px tall, border-radius 6px, overflow hidden, display flex (no wrap), border 1.5px solid #D0CCC4, cursor pointer, background transparent, padding 0, flex-shrink 0, flex-direction column, align-items center. The top portion (the two-color strip) is 32px tall, the label below.
  - Actually: the swatch button contains two halves side by side (each 50% width, 32px tall) plus a label below. Total button: 48px wide, auto height. The two halves are inside a 48x32 container, label is outside/below.
- `.theme-swatch:hover`: border-color #9E8C6A, filter brightness(1.05)
- `.theme-swatch.selected`: border 2.5px solid #C8A96E
- `.theme-swatch:focus-visible`: outline 2px solid #C8A96E, outline-offset 2px
- `.swatch-half`: width 50%, height 32px
- `.swatch-label`: display block, font-size 12px, font-family 'Inter', sans-serif, color #4A4540, margin-top 4px, text-align center, font-weight 400, white-space nowrap
- `.theme-swatch.selected .swatch-label`: font-weight 600
- On viewports under 480px, add mask-image fade on right edge of `.theme-picker-row`: `mask-image: linear-gradient(to right, black 85%, transparent)`

4. **Add carousel viewport CSS**:
- `.carousel-viewport`: width 420px, height 420px, position relative, overflow hidden, border-radius 4px, border 0.5px solid #D0CCC4, margin 0 auto
- `.carousel-container`: position relative (wrapper for arrows + viewport)

5. **Add slide frame CSS** (1080x1080 at full resolution):
- `.slide-frame`: width 1080px, height 1080px, position absolute, top 0, left 0, `transform: scale(0.389)`, `transform-origin: top left`, display flex, flex-direction column, justify-content space-between, padding 82px 92px 78px, background-color var(--slide-bg), opacity 0, `transform: scale(0.389) translateX(60px)`, transition `opacity 0.35s ease, transform 0.35s ease`, pointer-events none
- `.slide-frame.active`: opacity 1, `transform: scale(0.389) translateX(0)`, pointer-events auto
- `.slide-frame.exit`: opacity 0, `transform: scale(0.389) translateX(-60px)`

6. **Add slide content CSS** (inside the 1080x1080 canvas, using CSS custom properties):
- `.slide-top-row`: display flex, align-items center, justify-content space-between
- `.slide-badge`: font-size 24px, font-weight 400, letter-spacing 0.14em, text-transform uppercase, color var(--slide-muted), font-family var(--slide-font-body)
- `.slide-num`: font-size 24px, font-weight 400, color var(--slide-muted), letter-spacing 0.06em, font-family var(--slide-font-body)
- `.slide-body`: flex 1, display flex, flex-direction column, justify-content center, gap 14px
- `.slide-title`: font-family var(--slide-font-heading), font-size 62px, font-weight 600, line-height 1.25, color var(--slide-heading-color)
- `.slide-type-cover .slide-title`: font-size 72px, font-style italic, font-weight 400
- `.slide-accent-rule`: width 82px, height 5px, background var(--slide-accent), border-radius 2px
- `.slide-canvas[data-theme="minimal"] .slide-accent-rule`: height 1px, border-radius 0
- `.slide-canvas[data-theme="deep-space"] .slide-accent-rule`: box-shadow `0 0 8px rgba(74,158,255,0.4)`
- `.slide-canvas[data-theme="tech"] .slide-accent-rule`: box-shadow `0 0 8px rgba(0,212,170,0.4)`
- `.slide-text`: font-size 34px, line-height 1.7, color var(--slide-body-color), font-family var(--slide-font-body), font-weight 400
- `.slide-cta-text`: font-size 62px, font-weight 600, color var(--slide-heading-color), font-family var(--slide-font-heading)
- Corner SVG for Editorial only: `.slide-canvas[data-theme="editorial"] .slide-corner-tl, .slide-canvas[data-theme="editorial"] .slide-corner-br`: position absolute, width 48px, height 48px, pointer-events none. `.slide-corner-tl`: top 0, left 0. `.slide-corner-br`: bottom 0, right 0, transform rotate(180deg). Hide corners for non-editorial: `.slide-corner-tl, .slide-corner-br`: display none. `.slide-canvas[data-theme="editorial"] .slide-corner-tl, .slide-canvas[data-theme="editorial"] .slide-corner-br`: display block.

Do NOT remove any existing Phase 1 CSS rules. Add all new CSS after the existing hashtag-pill rule.
  </action>
  <verify>
    <automated>grep -c "data-theme" index.html | xargs test 8 -le</automated>
  </verify>
  <acceptance_criteria>
    - index.html @import line contains "Bitter" AND "JetBrains+Mono" AND "Playfair+Display" AND "Space+Grotesk" AND "DM+Sans"
    - index.html contains `[data-theme="nature"]` with `--slide-bg: #F2F5EE`
    - index.html contains `[data-theme="deep-space"]` with `--slide-bg: #0E0F14`
    - index.html contains `[data-theme="minimal"]` with `--slide-bg: #FFFFFF`
    - index.html contains `[data-theme="sunset"]` with `--slide-bg: #FBF3E8`
    - index.html contains `[data-theme="ocean"]` with `--slide-bg: #F0F4F7`
    - index.html contains `[data-theme="botanical-dark"]` with `--slide-bg: #0D1F0F`
    - index.html contains `[data-theme="tech"]` with `--slide-bg: #111418`
    - index.html contains `.slide-canvas` with `--slide-bg: #FAF8F4` (editorial default)
    - index.html contains `.theme-picker-row` with `overflow-x: auto`
    - index.html contains `.theme-swatch.selected` with `#C8A96E`
    - index.html contains `.carousel-viewport` with `width: 420px` and `height: 420px`
    - index.html contains `.slide-frame` with `width: 1080px` and `height: 1080px` and `scale(0.389)`
    - index.html contains `.slide-type-cover .slide-title` with `font-size: 72px`
    - index.html contains `.slide-accent-rule` with `var(--slide-accent)`
    - All existing Phase 1 CSS rules (.upload-zone, .slide-card, .results-header, etc.) are still present
  </acceptance_criteria>
  <done>All 8 theme CSS custom property sets defined, swatch picker styled, carousel viewport and slide frame CSS complete, all fonts loading via Google Fonts CDN</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite renderResults() to build carousel preview with theme picker and slide frames</name>
  <files>index.html</files>
  <read_first>
    - index.html (after Task 1 CSS additions -- read current full state)
    - .planning/phases/02-theme-selection-preview/02-RESEARCH.md (buildThemePicker code example, buildSlideDOM code example, getSlideType function, theme switch JS pattern)
    - .planning/phases/02-theme-selection-preview/02-CONTEXT.md (page flow, slide type variants, Editorial pre-selected)
    - .planning/phases/02-theme-selection-preview/02-UI-SPEC.md (page flow section, component inventory, slide type variants, copywriting contract)
    - solar_fuels_carousel_v2.html (reference carousel DOM structure, corner SVG markup)
  </read_first>
  <action>
In the `<script>` block of index.html, rewrite the `renderResults()` function and add supporting functions. Keep `parseMarkdown()`, `runParserTests()`, and all file handling code unchanged.

**Add these new functions before `renderResults()`:**

1. `getSlideType(index, totalSlides)` -- returns 'cover' if index===0, 'cta' if index===totalSlides-1, else 'body'.

2. `buildSlideDOM(slide, index, totalSlides, field)` -- creates a 1080x1080 slide-frame div using safe DOM construction (createElement/textContent, NO innerHTML). Structure:
   - `div.slide-frame` (add class 'active' if index===0, set `data-slide-index` attribute)
   - Corner SVGs for Editorial theme: Create two SVG elements (`.slide-corner-tl` and `.slide-corner-br`) using `document.createElementNS('http://www.w3.org/2000/svg', 'svg')`. The SVG viewBox is "0 0 48 48" and contains a path `d="M0 0 L48 0 L48 4 L4 4 L4 48 L0 48 Z" fill="#C8A96E" opacity="0.35"` and a circle `cx="4" cy="4" r="3" fill="#C8A96E" opacity="0.55"`. These are always in the DOM but CSS hides them for non-editorial themes.
   - `.slide-top-row` containing:
     - `span.slide-badge` with textContent = field || 'Science Drop'
     - `span.slide-num` with textContent = zero-padded index+1 + ' \u2014 ' + zero-padded totalSlides (e.g., "01 \u2014 06")
   - `.slide-body.slide-type-{type}` containing:
     - `div.slide-title` with textContent = slide.title
     - `div.slide-accent-rule`
     - If type === 'body': `div.slide-text` with textContent = slide.body
     - If type === 'cta': `div.slide-cta-text` with textContent = slide.body || 'Follow for daily science drops.'
     - If type === 'cover': `div.slide-text` with textContent = slide.body (the hook text)

3. `buildThemePicker(container, onSelect)` -- creates theme swatch row using safe DOM construction. Theme data array:
   ```
   [
     { id: 'editorial', name: 'Editorial', bg: '#FAF8F4', accent: '#C8A96E' },
     { id: 'nature', name: 'Nature', bg: '#F2F5EE', accent: '#6A8A5A' },
     { id: 'deep-space', name: 'Deep Space', bg: '#0E0F14', accent: '#4A9EFF' },
     { id: 'minimal', name: 'Minimal', bg: '#FFFFFF', accent: '#1A1A1A' },
     { id: 'sunset', name: 'Sunset', bg: '#FBF3E8', accent: '#C4602A' },
     { id: 'ocean', name: 'Ocean', bg: '#F0F4F7', accent: '#2A8A8A' },
     { id: 'botanical-dark', name: 'Botanical Dark', bg: '#0D1F0F', accent: '#D4C9A8' },
     { id: 'tech', name: 'Tech', bg: '#111418', accent: '#00D4AA' }
   ]
   ```
   Each swatch is a `button.theme-swatch` containing:
   - A `div` container (48px x 32px, display flex) with two `div.swatch-half` children. Left half has `style.backgroundColor = theme.bg`, right half has `style.backgroundColor = theme.accent`.
   - A `span.swatch-label` with textContent = theme.name.
   - Click handler calls `onSelect(theme.id)` which sets `data-theme` on `.slide-canvas`, updates selected class on swatches (remove `.selected` from all, add to clicked one).
   Pre-select the 'editorial' swatch by adding `.selected` class to it on construction.

4. **Rewrite `renderResults(parsed, filename)`** to build the new page flow (per CONTEXT.md). The function should:
   a. Hide uploadZone, show #results, clear #results content via `textContent = ''`.
   b. Build and append the **results header row** (reuse existing pattern: div.results-header with span.filename-pill and button.upload-new-link). The "Upload new file" button handler resets view: shows uploadZone, hides results, clears results, resets fileInput.value, and removes the keydown listener for arrow keys.
   c. Build and append the **theme picker row** by calling `buildThemePicker(results, onSelectCallback)`. The onSelect callback does: `document.querySelector('.slide-canvas').setAttribute('data-theme', themeId)` and updates swatch selected states.
   d. Build and append the **carousel container** (div.carousel-container) containing:
      - `div.carousel-viewport` containing `div.slide-canvas[data-theme="editorial"]`
      - For each slide in parsed.slides, call `buildSlideDOM(slide, index, parsed.slides.length, parsed.field)` and append to `.slide-canvas`
      - Navigation arrows and dots will be added in Plan 02 -- for now, just the viewport + slides
   e. Build and append **caption section** (reuse existing pattern: div.section-heading "Caption" + div.caption-block with line breaks preserved via createElement('br') + createTextNode).
   f. Build and append **hashtags section** (reuse existing pattern: div.section-heading "Hashtags" + div.hashtags-container with span.hashtag-pill for each tag).
   g. Do NOT render the Phase 1 metadata row or "Slides (N)" section heading -- these are replaced by the carousel.
   h. Do NOT render Phase 1 slide cards (.slide-card elements) -- they are replaced by the carousel.

Store `currentSlide = 0` in a variable scoped to the renderResults closure (or module-level). Store a reference to the slide-canvas element for theme switching. The `goToSlide(idx)` function will be added in Plan 02 but the `currentSlide` variable should exist now.

**Important constraints:**
- Use ONLY safe DOM construction (createElement, textContent, createElementNS for SVGs). Do NOT use innerHTML or insertAdjacentHTML.
- Use ES5-compatible function syntax (function declarations, var, forEach) -- matching Phase 1 pattern.
- Keep parseMarkdown(), runParserTests(), and all file handling code completely unchanged.
  </action>
  <verify>
    <automated>grep -c "buildSlideDOM\|buildThemePicker\|getSlideType\|slide-canvas" index.html | xargs test 4 -le</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `function getSlideType(index, totalSlides)` returning 'cover', 'body', or 'cta'
    - index.html contains `function buildSlideDOM(slide, index, totalSlides, field)` using createElement (not innerHTML)
    - index.html contains `function buildThemePicker(container, onSelect)` creating 8 swatch buttons
    - renderResults function creates a div with class 'slide-canvas' and setAttribute('data-theme', 'editorial')
    - renderResults function creates div.carousel-viewport containing the slide-canvas
    - renderResults function does NOT create .slide-card elements or .metadata-row
    - renderResults function still creates .results-header with .filename-pill and .upload-new-link
    - renderResults function still creates .caption-block and .hashtags-container
    - index.html does NOT contain innerHTML anywhere in the script block
    - index.html still contains the unchanged parseMarkdown function and runParserTests function
    - The theme picker onSelect callback calls setAttribute('data-theme', themeId) on the slide-canvas element
    - A currentSlide variable is initialized to 0
    - Corner SVGs are created using createElementNS
  </acceptance_criteria>
  <done>Uploading a markdown file shows: filename pill + upload link, then 8 theme swatches (Editorial pre-selected), then a 420x420 carousel viewport showing the first slide at 1080x1080 scaled down, then caption and hashtags. Clicking a theme swatch instantly changes slide colors and fonts. The first slide is visible; no navigation controls yet (Plan 02).</done>
</task>

</tasks>

<verification>
1. Open index.html in a browser
2. Upload/drag the test markdown file
3. Verify: filename pill and "Upload new file" link appear at top
4. Verify: 8 theme swatches visible in a horizontal row, Editorial has gold border ring
5. Verify: 420x420 carousel viewport shows first slide with editorial styling (cream bg, gold accent, Lora font)
6. Verify: Click each of the 8 swatches -- slide colors and fonts change instantly, no page reload
7. Verify: Caption and hashtags sections visible below the carousel
8. Verify: No Phase 1 slide cards or metadata row visible
9. Verify: Browser console shows "PARSER TESTS: ALL PASSED" (Phase 1 tests still working)
10. Verify: No console errors
</verification>

<success_criteria>
- All 8 themes render with distinct colors and fonts when their swatch is clicked
- Carousel viewport displays at 420x420 with the first slide visible
- Editorial theme is auto-selected on file load
- Caption and hashtags render below the carousel
- Phase 1 parser tests still pass in console
- No innerHTML usage in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-selection-preview/02-01-SUMMARY.md`
</output>
